services:
  app:
    build: .
    ports:
      - "24500:8000"
    env_file: .env
    # command: "uvicorn main:app --reload --host 0.0.0.0"
    command: "python3 main.py"
    restart: always
    tty: true
    volumes:
      - .:/app

